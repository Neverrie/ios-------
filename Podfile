# Укажите минимальную версию iOS
platform :ios, '13.0'

target 'MyProject' do
  # Используйте динамические фреймворки
  use_frameworks!

  # Подключите библиотеку Charts
  pod 'DGCharts'

  # Другие зависимости (если нужны)
  # pod 'Alamofire'

  # Таргет для unit-тестов
  target 'MyProjectTests' do
    inherit! :search_paths
    # Подключите зависимости для тестов (если нужны)
  end

  # Таргет для UI-тестов
  target 'MyProjectUITests' do
    # Подключите зависимости для UI-тестов (если нужны)
  end
end
